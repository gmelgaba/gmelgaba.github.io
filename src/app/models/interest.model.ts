export class Interest {
  icon: string;
  title: string;
  description: string;

  constructor(se: any) {
    this.icon = se.icon;
    this.title = se.title;
    this.description = se.description;
  }
}
